version: 5
hierarchy:
  - name: Hardware-specific data
    paths:
    - "board/product/%{facts.dmi.board.product}.yaml"
    - "chassis/type/%{facts.dmi.chassis.type}.yaml"
    - "manufacturer/%{facts.dmi.manufacturer}/product/%{facts.dmi.product.name}.yaml"
